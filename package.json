{
  "name": "epochtalk-core-pg",
  "version": "0.9.8",
  "description": "Epochtalk Core for PostgreSQL",
  "main": "index.js",
  "scripts": {
    "db-migrate": "node_modules/.bin/db-migrate up",
    "db-drop": "dropdb $(cat database.json | ./node_modules/.bin/json $NODE_ENV.database) --if-exists",
    "db-create": "createdb $(cat database.json | ./node_modules/.bin/json $NODE_ENV.database)",
    "test-migrate": "./node_modules/.bin/db-migrate up -e test",
    "test-fixtures": "NODE_ENV=test npm run db-drop && NODE_ENV=test npm run db-create && npm run test-migrate && NODE_ENV=test node_modules/.bin/lab --flat -r html -o test/seed/coverage.html test/seed/test-fixtures.js",
    "test-report": "NODE_ENV=test npm run db-drop && NODE_ENV=test npm run db-create && npm run test-migrate && NODE_ENV=test node_modules/.bin/lab --flat -r html -o test/coverage.html",
    "test": "NODE_ENV=test npm run db-drop && NODE_ENV=test npm run db-create && npm run test-migrate && NODE_ENV=test node_modules/.bin/lab --flat -r console"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/epochtalk/core-pg.git"
  },
  "author": "James Wang <jw@slickage.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/epochtalk/core-pg/issues"
  },
  "homepage": "https://github.com/epochtalk/core-pg",
  "dependencies": {
    "bcrypt": "^0.8.0",
    "bluebird": "^2.3.10",
    "brototype": "0.0.2",
    "db-migrate": "^0.8.0",
    "faker": "^2.1.2",
    "json": "^9.0.3",
    "lodash": "^2.4.1",
    "pg": "^3.6.2"
  },
  "devDependencies": {
    "code": "^1.3.0",
    "lab": "^5.5.0"
  }
}
